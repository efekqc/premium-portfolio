# --- Web framework ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0

# --- ORM (async) ---
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29,<1.0
greenlet>=3.0           # required by SQLAlchemy's async bridge

# --- Validation / settings ---
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0

# --- Migrations ---
alembic>=1.13,<2.0

# --- Auth helpers (stdlib `secrets` is enough for v1; nothing to add) ---
